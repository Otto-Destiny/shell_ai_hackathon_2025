TITLE = "Model Insights"
DESCRIPTION = "Model health, per-property score reporting, and interpretability surfaces."

def section_metadata() -> dict[str, str]:
    return {"title": TITLE, "description": DESCRIPTION}

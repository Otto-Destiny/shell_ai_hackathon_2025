TITLE = "Fuel Registry"
DESCRIPTION = "Registry for components, blends, templates, and persisted records."

def section_metadata() -> dict[str, str]:
    return {"title": TITLE, "description": DESCRIPTION}
